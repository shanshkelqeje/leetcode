/**
 * @param {number} target
 * @param {number[]} nums
 * @return {number}
 */
var minSubArrayLen = function(target, nums) {
    // Using two-pointer
    left = 0;
    right = nums.length;
    
};

target = 15;
nums = [1, 2, 3, 4, 5];

console.log(minSubArrayLen(target,nums));